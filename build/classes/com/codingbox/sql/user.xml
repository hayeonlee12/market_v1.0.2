<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="User">
<!-- 	<select id="join" parameterType="product">
		INSERT INTO tbl_product VALUES 
			(#{productid}, #{productname}, #{price},#{productimg})
	</select> -->
	
	<select id="login" parameterType="product" resultType="_int">
		SELECT count(*) FROM TBL_PRODUCT tp WHERE PRODUCTID = #{productid}
	</select>
	
</mapper>











